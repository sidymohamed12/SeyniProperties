# =============================================================================
# Seyni Properties (Imani) - Configuration Environment Variables
# =============================================================================
# Copier ce fichier vers .env et remplir les valeurs réelles
# IMPORTANT: Ne jamais commiter le fichier .env dans git
# =============================================================================

# -----------------------------------------------------------------------------
# DJANGO CORE (REQUIS)
# -----------------------------------------------------------------------------

# CRITICAL: Générer une nouvelle clé avec:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-CHANGE-THIS-REQUIRED

# Debug mode (True pour dev, False pour production)
DEBUG=False

# Hosts autorisés (séparés par des virgules)
ALLOWED_HOSTS=imani.seyni.sn,localhost,127.0.0.1

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------

# PostgreSQL Database URL (Railway configure automatiquement)
# Format: postgresql://user:password@host:port/dbname
# Laisser vide pour SQLite en développement local
DATABASE_URL=

# -----------------------------------------------------------------------------
# RAILWAY DEPLOYMENT (Auto-configuré par Railway)
# -----------------------------------------------------------------------------

# Ces variables sont automatiquement définies par Railway
# RAILWAY_ENVIRONMENT=production
# PORT=8000

# -----------------------------------------------------------------------------
# EMAIL CONFIGURATION (REQUIS pour production)
# -----------------------------------------------------------------------------

# Backend email
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# Configuration SMTP Gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True

# IMPORTANT: Utiliser un App Password Gmail, pas votre mot de passe principal
# Guide: https://support.google.com/accounts/answer/185833
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password

# Email d'envoi par défaut
DEFAULT_FROM_EMAIL=Imani Properties <noreply@imaniproperties.com>

# -----------------------------------------------------------------------------
# COMPANY INFORMATION (Personnalisable)
# -----------------------------------------------------------------------------

COMPANY_NAME=Imani Properties
COMPANY_ADDRESS=Dakar, Sénégal
COMPANY_PHONE=+221 XX XXX XX XX
COMPANY_EMAIL=contact@imaniproperties.com

# -----------------------------------------------------------------------------
# NOTIFICATIONS - TWILIO (SMS & WhatsApp) (OPTIONNEL)
# -----------------------------------------------------------------------------

# Compte Twilio: https://www.twilio.com/console
# Compte d'essai gratuit disponible
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# WhatsApp Business via Twilio (optionnel)
TWILIO_WHATSAPP_NUMBER=

# -----------------------------------------------------------------------------
# PAYMENT METHODS APIs (OPTIONNEL - pour futures intégrations)
# -----------------------------------------------------------------------------

# Orange Money API
ORANGE_MONEY_API_KEY=
ORANGE_MONEY_MERCHANT_ID=

# Wave API
WAVE_API_KEY=
WAVE_MERCHANT_ID=

# -----------------------------------------------------------------------------
# CLOUD STORAGE (OPTIONNEL - par défaut utilise stockage local)
# -----------------------------------------------------------------------------

# Pour AWS S3 (production recommandée)
# USE_S3=True
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=seyni-properties-media
# AWS_S3_REGION_NAME=eu-west-3

# -----------------------------------------------------------------------------
# MONITORING & LOGGING (Production)
# -----------------------------------------------------------------------------

# Sentry pour tracking des erreurs
# SENTRY_DSN=https://your-sentry-project-dsn

# Niveau de logs
DJANGO_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# CACHE (OPTIONNEL)
# -----------------------------------------------------------------------------

# Redis pour cache et sessions (Railway peut fournir REDIS_URL)
# REDIS_URL=redis://host:port/db

# -----------------------------------------------------------------------------
# FRONTEND/CORS (Si applicable)
# -----------------------------------------------------------------------------

# URL du frontend (si séparé)
# FRONTEND_URL=https://app.imaniproperties.com

# CORS origins autorisés
# CORS_ALLOWED_ORIGINS=https://app.imaniproperties.com

# =============================================================================
# NOTES IMPORTANTES
# =============================================================================
#
# 1. SECRET_KEY (CRITIQUE):
#    - DOIT être unique et différente pour dev/staging/production
#    - Ne JAMAIS réutiliser la clé d'un autre environnement
#    - Générer: python manage.py shell -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# 2. EMAIL Configuration:
#    - Pour Gmail, activer l'authentification à 2 facteurs
#    - Créer un "App Password": https://myaccount.google.com/apppasswords
#    - NE PAS utiliser votre mot de passe Gmail principal
#
# 3. Twilio (pour SMS/WhatsApp):
#    - Gratuit en mode trial avec limitations
#    - Numéros virtuels disponibles pour tests
#    - Console: https://www.twilio.com/console
#
# 4. Railway Deployment:
#    - DATABASE_URL est fourni automatiquement par Railway
#    - Ne pas le définir manuellement
#    - Railway injecte aussi PORT, RAILWAY_ENVIRONMENT, etc.
#
# 5. Développement Local:
#    - Copier ce fichier: cp .env.example .env
#    - Remplir au minimum SECRET_KEY et DEBUG=True
#    - SQLite sera utilisé si DATABASE_URL est vide
#    - EMAIL_BACKEND peut être console.EmailBackend pour tests
#
# 6. Sécurité:
#    - .env est dans .gitignore (ne JAMAIS commiter)
#    - Renouveler les secrets tous les 6-12 mois
#    - Utiliser des valeurs différentes pour chaque environnement
#    - En cas de compromission, regénérer IMMÉDIATEMENT
#
# =============================================================================